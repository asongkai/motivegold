// mobile_file_picker.dart (stub for mobile platforms)
class WebFilePicker {
  static Future<String?> pickImage() async {
    // This should never be called on mobile platforms
    // The main widget handles mobile image picking differently
    throw UnsupportedError('WebFilePicker is only available on web platforms');
  }
}